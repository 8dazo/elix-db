defmodule ElixDb do
  @moduledoc """
  Elixir vector database: collections, points (upsert/get/delete), exact k-NN search
  (cosine or L2), file persistence, and optional HTTP API.

  Use `ElixDb.CollectionRegistry` to create collections and `ElixDb.Store` for
  upsert, search, get, and delete. See the [README](https://github.com/8dazo/elix-db)
  and [HexDocs](https://hexdocs.pm/elix_db) for full API documentation.
  """
end
